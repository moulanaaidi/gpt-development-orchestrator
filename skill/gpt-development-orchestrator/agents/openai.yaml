interface:
  display_name: "GPT Development Orchestrator"
  short_description: "Thin planning/review; GPT-6 Luna builds and verifies."
  default_prompt: "Use $gpt-development-orchestrator with the thin-root workflow. Reuse an approved spec when present; otherwise plan once, give one coherent bundle to GPT-6 Luna, wait for completion, and review once."
policy:
  allow_implicit_invocation: true
